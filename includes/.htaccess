# Bloqueia acesso direto aos arquivos internos (eles só devem ser usados via require/include)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
